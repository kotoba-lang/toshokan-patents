.cpcache/
.clj-kondo/
.lsp/
*.log

# kotobase ingest actor identity — Ed25519 private key material. NEVER commit.
# (ADR-2608138400: a gitignored actor identity is invisible to `git status`, so
# survey tooling must be told about it rather than left to rediscover it as WIP.)
scripts/.kotobase-ingest-*-identity.hex
